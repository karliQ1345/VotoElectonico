@model VotantesComprobanteVm
@{
    ViewData["Title"] = "Voto Registrado";
    ViewData["BodyClass"] = "layout-root layout-votante";
    ViewData["Navbar"] = "None";
    ViewData["Footer"] = false;
    ViewData["MainClass"] = "layout-main p-0";
}

<div class="vote-shell">
    <div class="vote-card bg-white p-4 p-md-5 rounded shadow">

        <h2 class="mb-3 text-success">Voto registrado</h2>

        <div class="alert alert-success">
            @Model.Mensaje
        </div>

        @if (Model.PapeletaEnviada)
        {
            <div class="card mt-3 border-info">
                <div class="card-body">
                    <h5 class="card-title text-info">Comprobante enviado</h5>
                    <p class="card-text">Su papeleta digital ha sido enviada a: <b>@Model.EmailEnmascarado</b></p>
                    <hr />
                    <p class="text-muted" style="font-size: 0.85rem;">
                        Si no recibe el correo en unos minutos, revise su carpeta de Spam o acérquese al 
                        <strong>CNE (Consejo Nacional Electoral)</strong> para solicitar su certificado físico.
                    </p>
                </div>
            </div>
        }
        else
        {
            <div class="alert alert-warning mt-3">
                Su voto fue registrado, pero hubo un inconveniente al enviar el correo. Por favor, solicite su comprobante en las mesas del CNE.
            </div>
        }

        <div class="d-grid gap-2 mt-4">
            <a class="btn btn-dark" asp-controller="Home" asp-action="Index">Finalizar y Salir</a>
        </div>

    </div>
</div>
